# Git & VCS
.git
.gitignore

# Python
.venv
venv
__pycache__
*.py[cod]
.pytest_cache
.ruff_cache
.mypy_cache
*.egg-info
dist
build

# Next.js (built by web/Dockerfile with its own context rules)
web/node_modules
web/.next
web/out

# Env & secrets (inject at runtime / compose)
.env
.env.*
!.env.*.example

# IDE & OS
.idea
.vscode
.DS_Store
*.log

# Tests & coverage (not needed in runtime images)
tests
htmlcov
.coverage

docs

# Agent / Playwright local artifacts
playwright-report
test-results
blob-report
